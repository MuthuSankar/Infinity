<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lv.infinity.app">
    <!-- To auto-complete the email text field in the login form with the User's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Shop_Category"
            android:label="@string/title_activity_shop__category"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".welcomepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityHome" />
        <activity
            android:name=".EnglishActivity"
            android:label="@string/title_activity_english" />
        <activity
            android:name=".EnglishActivityLogin"
            android:label="@string/title_activity_english" />
        <activity
            android:name=".LatvianActivity"
            android:label="@string/title_activity_latvian" />
        <activity
            android:name=".RussianActivity"
            android:label="@string/title_activity_russian" />
        <activity
            android:name=".Stats_en"
            android:label="@string/title_activity_stats_en" />
    </application>

</manifest>